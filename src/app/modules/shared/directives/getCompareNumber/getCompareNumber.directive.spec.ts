/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { GetCompareNumberDirective } from './getCompareNumber.directive';

describe('Directive: GetCompareNumber', () => {
  it('should create an instance', () => {
    const directive = new GetCompareNumberDirective();
    expect(directive).toBeTruthy();
  });
});
